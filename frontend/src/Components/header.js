import React from 'react';
//import pessoa from '../../../../backend/src/routes/pessoas'

const Header = ({ title }) => (
    <header>
        <h1 className="text-center">{title}</h1>
    </header>
);

export default Header;